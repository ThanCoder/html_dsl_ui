name: html_dsl_ui
description: A lightweight Dart DSL for building dynamic HTML interfaces with integrated JavaScript event handling and Fetch API support.
version: 0.1.0
repository: https://github.com/ThanCoder/html_dsl_ui.git

environment:
  sdk: ^3.10.7

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
